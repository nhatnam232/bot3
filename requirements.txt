# Thư viện cần thiết — host (Pterodactyl/uv...) tự đọc file này để cài
discord.py>=2.3.0
aiosqlite>=0.19.0
aiohttp>=3.8.0
python-dotenv>=1.0.0
# Nhạc: dùng Lavalink qua wavelink (bot chỉ nói TCP/WS -> né host chặn UDP)
wavelink>=3.5.0,<4.0.0
